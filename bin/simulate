#!/usr/bin/env ruby

require 'ants'

# Entry point here
include Ants

simulation = Sims::Simulation.new
simulation.start
simulation.analyze


# Play around a bit with the clusters
#
# clusters = simulation.analyzer.clusters
# class Array; def mode; sort_by {|i| grep(i).length }.last; end; end
# puts "mode: #{clusters.map{|c| c.map{|i| i.data[:target]}.mode }}"
#
# binding.pry

